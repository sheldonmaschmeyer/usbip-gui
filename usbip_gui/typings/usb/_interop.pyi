"""Stub for ``usb._interop`` — internal array conversion helpers."""

from typing import Any, Optional

def as_array(data_or_length: Optional[Any] = None) -> Any:
    """
    Return *data_or_length* as a ctypes array, or create one of that size.
    """
    ...
